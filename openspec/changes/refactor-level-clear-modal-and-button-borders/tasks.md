## 1. 全域選單按鈕邊框增強

- [x] 1.1 更新 `docs/menu.js` 中的 `createMenuButton`，為按鈕繪製 3px 珊瑚粉邊框（`0xff7675`），並在游標懸停時切換為金色邊框（`0xf9ca24`）。

## 2. 通關彈窗佈局與按鈕重構

- [x] 2.1 將 `showClearModal` 中的彈窗背景框更新為 `roundRect(200, 115, 400, 220, 20)`。
- [x] 2.2 重構一般關卡的標題與副標題位置（`clearSubText.text = ""`, 主標題在 `y = 160`）。
- [x] 2.3 重構挑戰模式的標題與副標題位置（`clearSubText.text = ""`, 主標題在 `y = 160`）。
- [x] 2.4 重構計時模式的標題與副標題位置（單行 `本次成績: mm:ss.ss (🎉 刷新紀錄!)` 位於 `y = 195`, 主標題在 `y = 150`）。
- [x] 2.5 統一彈窗按鈕寬度（`width = 220`），並對齊垂直位置（一般關卡 `y = 225` & `y = 280`；挑戰模式 `y = 250`；計時模式 `y = 265`）。

## 3. 測試與驗證

- [x] 3.1 在瀏覽器中分別測試一般關卡、挑戰模式與計時模式的過關彈窗畫面。
- [x] 3.2 驗證所有選單畫面的按鈕粉紅邊框與 Hover 懸停效果。
